{{#nav-breadcrumbs as |crumbs|}}
  {{#crumbs.item active=true}}{{entypo-icon "users"}} Users{{/crumbs.item}}
{{/nav-breadcrumbs}}

{{#paginate-models
  models=model
  routeName='users.index'
  totalCount=totalCount
  hasNextPage=hasNextPage
  endCursor=endCursor
  first=first
  after=after
  ascending=ascending
  sortBy=sortBy
  sortOptions=sortOptions
  as |model|
}}
  {{partial 'users/list'}}
{{/paginate-models}}
