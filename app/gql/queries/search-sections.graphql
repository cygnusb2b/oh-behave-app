#import 'oh-behave-app/gql/fragments/section'

query searchSections($propertyId: String!, $phrase: String!) {
  searchSections(propertyId: $propertyId, phrase: $phrase) {
    ...SectionFragment
  }
}
