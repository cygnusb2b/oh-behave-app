#import 'oh-behave-app/gql/fragments/taxonomy'

query searchTaxonomy($propertyId: String!, $phrase: String!) {
  searchTaxonomy(propertyId: $propertyId, phrase: $phrase) {
    ...TaxonomyFragment
  }
}
